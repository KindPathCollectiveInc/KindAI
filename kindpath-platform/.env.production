# Committed deliberately — these are the publishable-key equivalents,
# designed to be safe in a shipped browser bundle (RLS does the real
# access control, not secrecy of this key). This is what the GitHub
# Pages build and the Electron build both use. Never put the
# service_role / secret key here — see SECURITY.md.
VITE_SUPABASE_URL=https://aisxxlzcwdxaxkspenqy.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_V0DbBbXJ6FVjF3df9AQ9Yw_cqK3tLaL
